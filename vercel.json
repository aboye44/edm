{
  "buildCommand": "cd eddm-planner-updated && npm install && npm run build",
  "outputDirectory": "eddm-planner-updated/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
